stages:
  - publish

pages:
  stage: publish
  image: node:15

  before_script:
    - yarn install

  script:
    - yarn install
    - yarn build
    - mv dist public

  artifacts:
    paths:
      - public

  only:
    - master
